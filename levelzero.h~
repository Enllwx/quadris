#ifndef _LEVEL_ZERO_H_
#define _LEVEL_ZERO_H_

#include "difficulty.h"

class LevelZero: public Difficulty{
  int counter = 0;
public:
  LevelZero();
  LevelZero(std::shared_ptr<Grid> g);
  Block newBlock() override;
};

#endif
